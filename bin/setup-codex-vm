#!/usr/bin/env bash
# Abort on errors, undefined variables, or errors in pipelines
set -euo pipefail


export RUBY_BUILD_BINARY=1
asdf install ruby 3.2.2            # succeeds because a binary tarball exists

